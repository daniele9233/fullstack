ANSIBLE TESTATO ED ESEGUITO SU 

ansible [core 2.18.8]
python version = 3.12.3 (main, Aug 14 2025, 17:47:21) [GCC 13.3.0] (/usr/bin/python3)
jinja version = 3.1.2
VERSION="24.04.3 LTS (Noble Numbat)"


Step1:  Eseguire questi comandi per capire quali versione di rancher è compatibile con rke2 cosi da poter popolare le variabili


Ultime versioni di rancher stabili 

curl -s https://api.github.com/repos/rancher/rancher/releases | jq -r '.[] | select(.tag_name | test("^v[0-9]+\\.[0-9]+\\.[0-9]+$")) | .tag_name' | head -5


Ultime versioni di kubernetes 

curl -s https://api.github.com/repos/rancher/rke2/releases | jq -r '.[].tag_name' | grep -v rc | head -10


Inserire i certificati all'interno della dir

/roles/master1_helm_cert_manager_install/files


cacerts.pem
fullchain.pem
privkey.pem


PRODECURA DI INSTALLAZIONE

ansible-playbook -i inventory.ini site.yml


CLUSTER RANCHER A 3 NODI

Il cluster RKE2 locale che ospita Rancher e' definito dal gruppo [masters]
dell'inventario. Il PRIMO host del gruppo bootstrappa il cluster, gli altri si
uniscono a lui sulla porta 9345 (supervisor, non 6443). Il numero di nodi deve
essere dispari: 1 o 3, mai 2, perche' etcd ha bisogno del quorum.

Non c'e' nulla da cambiare nei ruoli per passare da 1 a 3 nodi: basta
aggiungere gli host a [masters]. Da li' derivano da soli il config.yaml di
ogni nodo, i tls-san del certificato API, l'upstream nginx verso Rancher e
l'IP usato per il fix DNS di cattle-cluster-agent.

Variabili in group_vars/all.yml:

  rke2_token                 vuoto (default) = automatico. Il primo master
                             bootstrappa senza token, RKE2 ne genera uno, e i
                             master successivi lo leggono da lui in
                             /var/lib/rancher/rke2/server/node-token e se lo
                             scrivono nel proprio config.yaml. Nessun segreto
                             nel repository e nessun passaggio manuale.
                             Valorizzarlo solo se il token deve essere noto in
                             anticipo: in quel caso viene scritto uguale sul
                             config.yaml di tutti i master prima del primo
                             avvio. Generalo con: openssl rand -hex 32
  rke2_supervisor_port       9345
  rke2_version               vuoto = ultima da get.rke2.io; valorizzare per
                             fissare la versione e rendere i run ripetibili
  rke2_cni                   deve essere identico su tutti i master
  rke2_disable               componenti packaged da disabilitare
  rke2_extra_tls_san         SAN extra (VIP o FQDN del load balancer)
  rke2_write_kubeconfig_mode permessi di rke2.yaml, vuoto = default RKE2
  rancher_endpoint_ip        IP a cui punta rancher_domain in /etc/hosts.
                             Default il primo master; con 3 master conviene
                             valorizzarlo con l'IP del bilanciatore nginx

Per riprodurre la configurazione del cluster di produzione (calico al posto
di canal):

  rke2_cni:
    - calico
  rke2_disable:
    - rke2-canal
    - cilium

I master vengono installati un nodo alla volta (serial: 1): ogni nodo attende
che il proprio API server risponda su /readyz prima di liberare il successivo.
Aggiungere un server etcd mentre il precedente non e' ancora entrato nel
quorum corrompe l'inizializzazione del cluster.


RIUTILIZZO DI MACCHINE GIA' USATE

site.yml e' un playbook di installazione da zero: crea un nuovo cluster via API
Rancher a ogni esecuzione. Su macchine gia' usate in un'installazione
precedente i nodi conservano lo stato vecchio e il cluster non si tira su:

  - /etc/cni/net.d contiene ancora il conflist del CNI precedente. Vince per
    ordine alfabetico su quello nuovo e ogni pod resta in ContainerCreating con
    'failed to find plugin "<cni>-cni" in path [/opt/cni/bin]'
  - /var/lib/rancher/agent conserva il vecchio Cattle ID, che il nuovo Rancher
    non riconosce: 401 durante il download della connection info

In quel caso lanciare il playbook con la pulizia dei nodi abilitata:

ansible-playbook -i inventory.ini site.yml -e clean_nodes_before_join=true

La pulizia gira su [masters], [new_managers] e [workers] prima di qualsiasi
installazione, esegue gli uninstall ufficiali di rke2 e del
rancher-system-agent e azzera le directory di stato. Include i master perche'
su di loro "helm install rancher" e "kubectl create namespace cattle-system"
falliscono se Rancher e' gia' installato.

E' distruttiva ed e' disattivata di default: NON usarla su un cluster
funzionante.



